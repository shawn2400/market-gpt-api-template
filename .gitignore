# --- Env / Keys (אל תעלה מפתחות לריפו)
.env
*.env
*.key
*.pem

# --- Data / Logs
saved_trades.json
pnl_tracker.json
*.log

# --- כן משאירים את רשימת המעקב
!watchlist.json

# --- Python caches
__pycache__/
*.pyc
.cache/

# --- Virtualenv / Dev
venv/
.idea/
.vscode/
*.iml

# --- OS Files
.DS_Store
Thumbs.db

# --- Reports & snapshots (נשמר מקומית בלבד)
*.pdf
static/reports/
static/snapshots/
static/cache/        # ← חדש: cache כבד

# --- Tests (לא נעלה ל-prod repo)
tests/
test_*.py

# --- Local runners / helpers (לא דרוש ל-prod)
run_executor.py
scan_runner.py
check_env.py
get_chat_id.py

# --- Build artifacts
dist/
build/
*.egg-info/

# --- Git metadata
.gitignore








