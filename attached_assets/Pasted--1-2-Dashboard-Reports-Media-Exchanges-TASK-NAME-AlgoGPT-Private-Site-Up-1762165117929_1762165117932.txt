הודעה 1/2 — אתר בלבד (Dashboard + Reports + Media + Exchanges)

TASK NAME: AlgoGPT — Private Site Upgrade (Only Website Add-Ons, 4GB-safe)

מטרה: לשדרג את האתר הפרטי בלבד: דשבורד חי, דוחות 2×יום באתר, סינון חדשות לפני הצגה, תמונות/וידאו, ו־Settings→Exchanges לניהול בורסות דינמי בזמן אמת. טלגרם נשאר רק לאישורי טרייד.

Deliverables (Frontend בלבד):

Navigation & Structure

8 לשוניות: Markets / Trades / Positions / News / Health / FinOps / Reports / Settings.

Header “בס״ד + ברכה” + שעון Israel (Asia/Jerusalem), באנר “What’s new”.

Feature Flags לכל ווידג׳ט (כיבוי/הדלקה חם).

Markets

Live Price Table ל־531 סמלים עם Virtualization.

חיפוש/סינון <100ms (עד 10k רשומות), Skeleton loaders.

Virtualized Charts + Web Workers (Sharpe/Sortino/Drawdown).

News (Front) — רק פריטים מסוננים מראש

הצגת חדשות אחרי סינון Pre-Ingest (ראה ליבה).

Media Pipeline (Site):

תמונות: thumbnails 200/800px, srcset, lazy-load + blur-up, long cache.

וידאו: H.264 + HLS (360/720), poster אוטומטי, mute-autoplay + click-to-unmute, pause כשמחוץ לפריים.

הגשה דרך /media/* (CDN/Static), Signed URLs אופציונלי.

Secure Rendering: CSP allowlist למדיה, rel="noopener"/sandbox, בדיקת MIME/גודל, EXIF strip.

Health & FinOps

כרטיסי p95/p99, CPU/MEM, SSE status + Error-Budget tile.

Token Burn, Cache-Hit, Burn-Forecast 24h, Budget Alerts.

Reports (Site-Only)

אוטומטי 09:00 / 21:00 + כפתור “Generate Now”.

היסטוריה מלאה, PDF/CSV download, Audit Trail חתום (hash/timestamp/author/path).

אין דוחות/התראות לטלגרם.

Settings → Exchanges (ניהול בורסות דינמי)

טבלה: Exchange / Type (Spot/Futures) / Status / Health / Last Sync / Priority.

פעולות: Add / Edit / Enable/Disable / Remove(archive) / Test Connection / Sync Now.

Add/Edit Modal: Binance/Bybit/OKX/Custom, Testnet On/Off, Capabilities (Spot/Futures/Margin), Rate-Limit Profile (Safe/Normal/Aggressive), Priority (1–5), Default venue toggle, Notes.

מפתחות לא נשמרים ב-Frontend — הצגה כ-****last4 בלבד; כפתור Rotate שולח בקשה לשרת.

Security & Mobile

Replit Auth RBAC: Admin=full / Viewer=read-only.

PWA (install + offline shell), CSP/HSTS + SameSite=Strict, Dark/Light.

SLOs (אתר):

LCP < 2.0s, TTI < 3.0s, bundle ≤ 250KB gzip.

SSE uptime ≥ 99%, p95 < 1.6s, p99 < 2.8s.

Search/Filter < 100ms (10k rows).

Media: teaser image ≤ 300ms; HLS 720p ≤ 1.2s start; CLS יציב.

Settings→Exchanges: פתיחה ≤ 800ms, Test Connection ≤ 2.0s.

Acceptance (אתר):

8 לשוניות פעילות; מדדים חיים מתעדכנים; אין שגיאות קונסולה.

דוחות נוצרים 09:00/21:00 + “Generate Now” ≤ 30s; PDF/CSV תקין; Audit Trail חתום.

חדשות מוצגות רק לאחר סינון Pre-Ingest; מדיה נטענת חלק; אין הפרות CSP.

Settings→Exchanges: הוספה/עריכה/Enable/Disable/Remove ו-Test Connection עובדים; אין הדלפת מפתחות; Rotate מופעל דרך השרת.

טלגרם: רק Approve/Reject (אין דוחות/התראות אחרות).

Go-Live Runbook (10 דק׳):

npm run lint && npm run build → ירוק; .env עובר Zod.

Feature Flags: כבה/הדלק News/FinOps/Exchanges → אין refresh/שגיאות.

נתק רשת 30ש׳ → SSE חוזר (backoff+jitter).

Generate Now → היסטוריה + hash; הורדת PDF/CSV.

הוסף Binance/Bybit, Test Connection → PASS עם latency.

ודא בטלגרם: רק Approve/Reject