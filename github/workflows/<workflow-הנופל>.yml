- name: Setup Python
  uses: actions/setup-python@v5
  with:
    python-version: '3.11'

- name: Install ruff
  run: |
    python -m pip install --upgrade pip
    python -m pip install ruff==0.6.7

- name: Ruff version
  run: ruff --version

- name: Ruff check
  run: ruff check .
