from utils.ws_fallback import get_price

def execute_trade_live(symbol, side, entry=None, sl=None, tp=None, budget=100, leverage=10):
    # ????? ?????? ????
    price = entry if entry else get_price(symbol)
    if price is None:
        raise Exception(f'?? ???? ????? ???? ???? {symbol}')
    # ?????? ????? ????? (???? - ?????? ????? ???)
    print(f'???? ????? {side} ?? {symbol} ?-{price} (?????: {leverage}, ?????: {budget})')
    # ???? ???? ?????? (??? ???? ?? ???? ??????)
    return {"symbol": symbol, "side": side, "entry": price, "leverage": leverage, "budget": budget}
